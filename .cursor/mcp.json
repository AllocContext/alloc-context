{
  "mcpServers": {
    "alloc-context": {
      "command": "${workspaceFolder}/.venv/bin/alloc-context",
      "args": [
        "mcp",
        "--config",
        "${workspaceFolder}/config/config.yaml"
      ],
      "env": {
        "ALLOC_CONTEXT_DB": "${workspaceFolder}/state/alloccontext.db"
      },
      "cwd": "${workspaceFolder}"
    }
  }
}
